spring:
  datasource:
    url: jdbc:sqlite:/app/data/domu.db?busy_timeout=30000&journal_mode=WAL
  jpa:
    show-sql: false

app:
  upload:
    dir: /app/data/uploads
  base-url: ${BASE_URL:http://localhost:8080}
  jwt:
    secret: ${JWT_SECRET}
